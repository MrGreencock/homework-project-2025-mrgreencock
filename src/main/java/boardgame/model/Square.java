package boardgame.model;

/**
 * It is a enum class of squares. There are 4 types of squares: default, red (disk), blue (disk)
 * and black (the obstacle square).
 */
public enum Square {
    NONE,
    RED,
    BLUE,
    BLACK
}
